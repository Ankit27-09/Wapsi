{
  "name": "@rc/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Classification and policy proposals. The model's entire contribution to the system, behind a provider layer — Gemini or Groq over plain fetch, with every response validated.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@rc/core": "workspace:*",
    "@rc/db": "workspace:*",
    "zod": "^3.24.1"
  }
}
