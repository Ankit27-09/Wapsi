{
  "name": "@rc/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Classification. The LLM's entire contribution to the system, and the only package that talks to a model.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.121.0",
    "@rc/core": "workspace:*",
    "@rc/db": "workspace:*",
    "zod": "^3.24.1"
  }
}
