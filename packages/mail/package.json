{
  "name": "@rc/mail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dispatches the message a decision already produced, with the arithmetic that justified it. Composes nothing.",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": { "build": "tsc --build", "test": "vitest run" },
  "dependencies": { "@rc/core": "workspace:*", "zod": "^3.24.1" }
}
