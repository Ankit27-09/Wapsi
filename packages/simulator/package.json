{
  "name": "@rc/simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Seeded gateway simulator, ground-truth priors, and the batch generator. Structurally forbidden from importing @rc/policy.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "priors.truth.yaml"],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@rc/core": "workspace:*",
    "@rc/db": "workspace:*",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "fast-check": "^3.23.2"
  }
}
