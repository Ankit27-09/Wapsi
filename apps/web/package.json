{
  "name": "@rc/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only operations console. Server Components query Postgres directly — there is no API layer to keep in sync.",
  "scripts": {
    "dev": "next dev --port 3100",
    "build": "next build",
    "start": "next start --port 3100"
  },
  "dependencies": {
    "@rc/core": "workspace:*",
    "@rc/db": "workspace:*",
    "@rc/policy": "workspace:*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2"
  }
}
